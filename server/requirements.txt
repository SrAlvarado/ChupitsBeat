fastapi
uvicorn[standard]
python-multipart
yt-dlp
demucs
torchcodec   # requerido por torchaudio nuevo para guardar los stems (usa ffmpeg)

# Descubrimiento por similitud acústica
requests        # clientes HTTP (Deezer, descarga de previews)
librosa         # features clásicas (BPM, tonalidad, energía, brillo)
soundfile       # backend de lectura de audio para librosa
transformers    # embedding neuronal CLAP (laion/clap-htsat-unfused)
supabase        # caché persistente de perfiles (pgvector)
python-dotenv   # carga server/.env
playwright      # scrape de oyentes mensuales de Spotify (gratis); requiere:
                #   python -m playwright install chromium
